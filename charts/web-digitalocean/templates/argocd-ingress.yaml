apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.argocd_ingress.name }}
  namespace: {{.Values.argocd_ingress.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: {{ .Values.argocd_ingress.namespace }}
    server: {{ .Values.spec.destination.server }}
  project: {{ .Values.argocd.project }}
  source:
    path: charts/web-digitalocean/argocd-ingress
    repoURL: {{ .Values.spec.source.repoURL }}
    targetRevision: {{ .Values.spec.source.targetRevision }}
    helm:
      {{- if .Values.argocd_ingress.parameters }}
      parameters:
      {{- range $key, $value := .Values.argocd_ingress.parameters }}
        - name: {{$key}}
          value: {{$value | quote}}
      {{- end }}
      {{- end}}

  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true